import { ClientService } from '../client';
import { LoadPreviewOptions } from '.';
import { AuthStore, ConfigStore, UserStore } from '../../composables';
export declare class PreviewService {
    clientService: ClientService;
    configStore: ConfigStore;
    userStore: UserStore;
    authStore: AuthStore;
    constructor({ clientService, userStore, authStore, configStore }: {
        clientService: ClientService;
        userStore: UserStore;
        authStore: AuthStore;
        configStore: ConfigStore;
    });
    private get user();
    loadPreview(options: LoadPreviewOptions, cached?: boolean, silenceErrors?: boolean, signal?: AbortSignal): Promise<string | undefined>;
    private cacheFactory;
    private buildQueryString;
    private privatePreviewBlob;
    private publicPreviewUrl;
}
