export declare const parseVersion: (tag: string) => {
    major: number;
    minor: number;
    patch: number;
    pre: string;
};
/**
 * Compares two version strings.
 * Returns:
 * - negative number if a is less than b,
 * - positive number if a is greater than b,
 * - 0 if a and b are equal.
 */
export declare const compareVersions: (a: string, b: string) => number;
