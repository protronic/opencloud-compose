import { ClientService } from '../services';
import { Ref } from 'vue';
import { ArchiverCapability } from '@opencloud-eu/web-client/ocs';
import { UserStore } from '../composables';
interface TriggerDownloadOptions {
    files?: string[];
    fileIds?: string[];
    publicToken?: string;
    publicLinkPassword?: string;
}
export declare class ArchiverService {
    clientService: ClientService;
    userStore: UserStore;
    serverUrl: string;
    capability: Ref<ArchiverCapability>;
    available: Ref<boolean>;
    constructor(clientService: ClientService, userStore: UserStore, serverUrl: string, archiverCapabilities?: Ref<ArchiverCapability[]>);
    triggerDownload(options: TriggerDownloadOptions): Promise<string>;
    private buildDownloadUrl;
    private get url();
}
export {};
