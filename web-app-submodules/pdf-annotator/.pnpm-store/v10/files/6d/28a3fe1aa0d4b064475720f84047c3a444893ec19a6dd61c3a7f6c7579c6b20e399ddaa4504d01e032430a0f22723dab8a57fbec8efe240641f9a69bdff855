import { FolderLoader, FolderLoaderTask, TaskContext } from '../folderService';
import { Router } from 'vue-router';
export declare class FolderLoaderFavorites implements FolderLoader {
    isEnabled(): boolean;
    isActive(router: Router): boolean;
    getTask(context: TaskContext): FolderLoaderTask;
}
